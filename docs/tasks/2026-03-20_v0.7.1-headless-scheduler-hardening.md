# Task - v0.7.1 Headless Scheduler Hardening

Date: 2026-03-20  
Scope: docs-only first slice definition for `v0.7.1 ops`

## Goal

- Define the first concrete implementation slice for `v0.7.1 ops`.
- Limit that slice to scheduler registration hardening only, specifically moving away from `Interactive only` / `InteractiveToken` assumptions.
- Preserve the current operational contract: same task names, same cadence, same called scripts.
- Keep this note docs-first: it defines the first slice, but it is not yet implementation-authorizing until reviewed.

## Allowed changes

- Docs-only slice definition under `docs/tasks/`
- Read-only synthesis of:
  - `README.md`
  - `CIVILIZATION_RADAR_STATUS.md`
  - `docs/ops/BASELINE.md`
  - `docs/ops/RUNBOOK.md`
  - `docs/ops/weekly_rhythm.md`
  - `docs/context/civilization-radar-current-status.md`
  - `docs/tasks/2026-02-13_scheduler-recovery.md`
  - `docs/tasks/2026-03-18_post-closeout-triage.md`
  - `docs/tasks/2026-03-19_v0.7.1-ops-opening.md`
  - `ops/register_weekly_tasks.ps1`

## Do-not-touch list

- No runtime code changes in this planning round
- No scheduler mutation in this planning round
- No scoring, gate, kernel, persistence, DB schema, or pipeline flow changes
- No task renaming
- No cadence changes
- No changes to the scripts called by the three scheduler tasks
- No `WakeToRun` enablement in this first slice
- No broader operator tooling changes
- No observer or product-scope expansion

## First slice definition

- The first `v0.7.1 ops` slice is scheduler registration hardening only.
- The implementation target is the task registration path and its supporting verification/docs, so scheduled execution no longer depends on `Interactive only` / `InteractiveToken` assumptions.
- The three scheduler tasks must remain exactly:
  - `CivilizationRadar-WeekdaySnapshots`
  - `CivilizationRadar-FridayPromote`
  - `CivilizationRadar-MonthEndPipelineTag`
- The task cadence must remain exactly:
  - Mon-Thu 18:00: `CivilizationRadar-WeekdaySnapshots`
  - Fri 18:00: `CivilizationRadar-FridayPromote`
  - Daily 19:00: `CivilizationRadar-MonthEndPipelineTag`
- The called scripts must remain exactly:
  - `ops/collect_snapshots_weekday.ps1`
  - `ops/promote_latest.ps1`
  - `ops/month_end_release.ps1`
- This slice is about hardening how those tasks are registered and run under headless-capable conditions, not about changing what they do.

## Why this is the first slice

- The repo already records `Interactive only` / `InteractiveToken` as the remaining planned ops follow-up after closeout and Batch A.
- Batch A resolved contract-level trust issues in render and promotion behavior on `2026-03-19`, which means the next safe ops question is scheduler reliability rather than artifact correctness.
- Headless-capable scheduler registration is operationally meaningful and still narrow enough to keep baseline comparability intact.

## Explicit deferrals

- `WakeToRun` enablement stays deferred in this slice. It may be evaluated later, but it is not part of the first hardening pass.
- Broader operator tooling stays deferred, including any larger control-plane, operator UX, or administrative workflow work.
- Observer/product expansion stays deferred.
- Any change to scoring, gate, kernel, persistence, DB schema, or pipeline flow stays out of scope.

## Eventual implementation verification requirements

1. Registration/query checks must be run for all three scheduler tasks after the change.
2. Verification must confirm the task names remain unchanged.
3. Verification must confirm the schedule remains unchanged.
4. Verification must confirm the called scripts remain unchanged.
5. Verification must confirm the task definitions no longer rely on `Interactive only` / `InteractiveToken` assumptions.
6. Verification must include a repo cleanliness check with `git status --short`.
7. Verification must confirm health checks remain green after the change, including:
   - scheduler task query sanity for all three tasks
   - `output/latest/reports/eval_quality.json` still present with `ok = true`
   - no new runtime pollution introduced by the scheduler-hardening batch

## Public API / contract impact

- None in this planning round.
- This slice definition does not change any public API, schema, output contract, or runtime contract by itself.
- The only new artifact in this batch is this task file.

## Results / notes

- This file opens the first concrete `v0.7.1 ops` slice boundary without starting implementation.
- The operative rule remains: define first, review second, authorize implementation batch only after that review.
- No runtime behavior, scheduler state, or canonical outputs were changed by this document.

## Follow-ups

- Review this slice definition before opening any implementation batch.
- If approved, implement only headless-capable scheduler hardening in the first `v0.7.1 ops` batch.
- Keep `WakeToRun`, broader tooling, and observer/product expansion deferred until after the first slice is completed and reviewed.
