# Task - v0.7.1 Ops Opening

Date: 2026-03-19  
Scope: docs-only opening note for the next post-Batch-A operational phase

## Goal

- Define the next approved post-Batch-A operational topic.
- Keep `Observation Baseline` safeguards intact while moving from closeout cleanup into the next ops phase.
- Frame `v0.7.1 ops` as scheduler and operator hardening, not feature expansion.

## Allowed changes

- Docs-only updates under `docs/tasks/`
- Read-only reference to:
  - `README.md`
  - `CIVILIZATION_RADAR_STATUS.md`
  - `docs/ops/BASELINE.md`
  - `docs/context/civilization-radar-current-status.md`
  - `docs/tasks/2026-03-18_observation-baseline-closeout.md`
  - `docs/tasks/2026-03-18_post-closeout-triage.md`
  - `docs/tasks/2026-03-19_blocking-operational-fixes.md`

## Do-not-touch list

- No runtime code changes
- No scheduler modifications in this opening note
- No pipeline flow changes
- No DB schema changes
- No scoring, gate, kernel, or persistence changes
- No observer-scope expansion
- No opening of a concrete implementation slice in this document

## Decision inputs

- Observation Baseline closeout passed in commit `4119d66`.
- `A. blocking operational fixes` completed in commit `ebb3e34`.
- The current scheduler is functioning, but remains:
  - `Interactive only`
  - `LogonType = InteractiveToken`
  - `WakeToRun = not enabled`
- `README.md` already names `v0.7.1 ops` as the planned follow-up for headless-capable scheduler hardening.
- `docs/tasks/2026-03-18_post-closeout-triage.md` kept `v0.7.1 ops` deferred until after Batch A.

## Recommended framing

- `v0.7.1 ops` is now the next candidate post-Batch-A phase.
- Its intended focus is headless-capable scheduler hardening and operator reliability.
- This note does not yet authorize implementation or define the first slice.
- The purpose of this note is to establish the boundary and rationale before new code changes begin.

## Verification

- This note is docs-only.
- `git status --short` after this change should show only this new task file plus the existing unrelated local files.

## Follow-ups

- Draft the first `v0.7.1` implementation slice after this opening note is reviewed.
- Keep broader observer or product expansion deferred until after the first `v0.7.1` slice is defined.
